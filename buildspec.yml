version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - sudo npm install
  build:
    commands:
      - echo "Building application..."
      
artifacts:
  files:
    - '**/*'
