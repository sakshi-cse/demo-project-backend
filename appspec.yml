version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html

hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 100

  ApplicationStart:
    - location: scripts/application_start.sh
      timeout: 300
 
        
 
